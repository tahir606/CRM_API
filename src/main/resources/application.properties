
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:3306/CRM_NEW

spring.datasource.username=crm
spring.datasource.password=crm123!@#
spring.jpa.hibernate.ddl-auto=update
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
server.port=4040
#server.address=192.168.100.210
server.tomcat.threads.max=200
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
# Hibernate
hibernate.dialect= org.hibernate.dialect.MySQL5Dialect
hibernate.show_sql=false
hibernate.hbm2ddl.auto=update
entitymanager.packagesToScan= com.example.CRM

## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=215MB
# for updated immutable
logging.level.org.hibernate.persister.entity= ERROR

## File Storage Properties
# All files uploaded through the REST API will be stored in this directory
#file.upload-dir=C:/Users/Bits/Documents
#server.tomcat.relaxed-query-chars= {,}

